apiVersion: v1
kind: Service
metadata:
  name: mysql-nodeport
  namespace: mysql
spec:
  type: NodePort
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 30306   # 这里指定一个固定端口（可选）
